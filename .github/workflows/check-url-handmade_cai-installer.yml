name: Check url handmade - Cai (Installer)

on:
  #schedule:
  #  - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  _0:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/0.py

  # ==========================================

  _1:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/1.py

  # ==========================================

  _2:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/2.py

  # ==========================================

  _3:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/3.py

  # ==========================================

  _4:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/4.py

  # ==========================================

  _5:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/5.py

  # ==========================================

  _6:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/6.py

  # ==========================================

  _7:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/7.py

  # ==========================================

  _8:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/8.py

  # ==========================================

  _9:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/9.py

  # ==========================================

  _a:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/a.py

  # ==========================================

  _b:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/b.py

  # ==========================================

  _c:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/c.py

  # ==========================================

  _d:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/d.py

  # ==========================================

  _e:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/e.py

  # ==========================================

  _f:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/f.py

  # ==========================================

  _g:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/g.py

  # ==========================================

  _h:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/h.py

  # ==========================================

  _i:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/i.py

  # ==========================================

  _j:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/j.py

  # ==========================================

  _k:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/k.py

  # ==========================================

  _l:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/l.py

  # ==========================================

  _m:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/m.py

  # ==========================================

  _n:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/n.py

  # ==========================================

  _o:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/o.py

  # ==========================================

  _p:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/p.py

  # ==========================================

  _q:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/q.py

  # ==========================================

  _r:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/r.py

  # ==========================================

  _s:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/s.py

  # ==========================================

  _t:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/t.py

  # ==========================================

  _u:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/u.py

  # ==========================================

  _v:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/v.py

  # ==========================================

  _w:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/w.py

  # ==========================================

  _x:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/x.py

  # ==========================================

  _y:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/y.py

  # ==========================================

  _z:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r environment/requirements.txt

    - name: Clone winget-pkgs
      run: |
        git clone https://github.com/DuckDuckStudio/winget-pkgs.git -b wingettools

    - name: Run Python script
      run: python auto_script/check/Cai_installer/z.py